#include "main.h"
/**
 * more_numbers - prints numbers
 * Return: void
 */
void more_numbers(void)
{
	int n = '0', t = '1', loop = 0, loop2 = 0
		counter = 0;

	while (loop < 10)
	{
		while (loop2 < 14)
		{
			if (counter = 10)
			{
				n = '0'
			}
			if (counter >= 10)
			{
				_putchar(t);
			}
			_putchar(n);
			n++;
			counter++;
			loop2++;
		}
		loop++;
		_putchar('\n');
	}
}			
